# 钱包总开关：必须为字符串 true 才会注册任何连接器（浏览器钱包 + WalletConnect 等）
# 留空 / false → connectors 为空，不拉起任何钱包；Wagmi / RainbowKit 仍保留在工程中
NEXT_PUBLIC_ENABLE_WALLETCONNECT=

# 上项为 true 时必填：Reown 项目 ID（https://cloud.reown.com），并配置 Allowed Domains
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=
